apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: openshift-gitops
spec:
  destination:
    namespace: keycloak
    server: 'https://kubernetes.default.svc'
  project: cluster-config
  source:
    path: components/apps/operators/keycloak/
    repoURL: https://github.com/turbra/cluster-config.git
    targetRevision: lab
  syncPolicy:
    automated:
      selfHeal: true
      prune: true